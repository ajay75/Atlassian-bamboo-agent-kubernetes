apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bamboo-agent-arm-pvc
  labels:
    app: bamboo-agent-arm
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 8Gi
